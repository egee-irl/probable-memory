// Generated by CoffeeScript 2.5.1
(function() {
  var Utils, exec, execSync, fs;

  ({execSync, exec} = require('child_process'));

  fs = require('fs');

  Utils = class Utils {
    static launchGame() {
      var dosbox, gamePath;
      dosbox = "/usr/bin/env dosbox -exit";
      gamePath = "/home/egee/Source/egeeirl/messy-drawer/public/games/haunted/HAUNTED.EXE";
      return exec(`${dosbox}  ${gamePath}`);
    }

    static checkForProcesses() {
      return execSync("ps").includes("dosbox");
    }

  };

  document.getElementById('game00').addEventListener('click', function() {
    if (!Utils.checkForProcesses()) {
      return Utils.launchGame();
    } else {
      return alert("The game is already running!");
    }
  });

}).call(this);
